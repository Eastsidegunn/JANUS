# 추적 매트릭스 — FR ID → 테스트

각 태스크 완료 시 해당 행을 추가한다. 테스트 없는 FR 구현 주장은 무효다.

| FR / 불변식 | 테스트 | 태스크 | 상태 |
|---|---|---|---|
| §3.1 의존 방향 (불변식: 의존은 아래로만, seam 수평 금지, collector-core 분리) | `tools/boundarylint/rules_test.go` + `make lint` 실그래프 검사 | T0 | green |
| §3.1 경계 린트 사각지대 (테스트 import, GOOS build tag, 미분류 패키지) | `tools/boundarylint/rules_test.go` (TestCheckTestImports, TestCheckRoguePackage, TestCheckDedup) + `tools/boundarylint/integration_test.go` | T0.1 | green |
| §5.1 이벤트 스키마 — 유효 샘플 수용·위반 샘플 거부 | `contracts/validate/validate_test.go` (TestValidateRecordValid/Invalid) | T1 | green |
| §5.2 와이어 프로토콜 — 방향별 검증(command/event), raw 필수, 합성 이벤트 `""` | `contracts/validate/validate_test.go` (TestValidateCommand, TestValidateEvent) | T1 | green |
| FR-OBS-01 — OTel all-zero trace/span ID 거부 | `contracts/validate/validate_test.go` (TestValidateRecordInvalid: all-zero 케이스) | T1 | green |
| FR-OBS-01/02 (부분) — 검증된 로그 snapshot을 원본 trace/span ID·부모 관계와 adapter/version/profile/usage/status 속성을 가진 결정적 OTel SpanData로 사영하고, noop 기본 및 export 실패를 append/replay와 격리 | `core/observe/observe_test.go` (TestProjectPreservesIDsParentAttributesAndIsDeterministic, TestProjectRejectsMissingVersionProfileAndUsageOverflow, TestExportFailureDoesNotBlockAppendOrReplayAndNilIsNoop) + `tools/boundarylint/rules_test.go` (OTel import를 core/observe로 한정) | T14-2 | green — Jaeger Linux 관통은 T14-5 잔여 |
| FR-CLI-05 — `hx dump-config`가 `policy.Merge`와 `policy.Evaluate`의 최종 정책 트리를 결정적으로 출력하고, 민감값은 `<redacted>`로 표시하며 host-only capability·경로를 노출하지 않음; 평가/파싱 오류는 stdout 0바이트 | `surfaces/hx/dump_config.go`, `surfaces/hx/dump_config_test.go` (redaction marker·원문 부재, 20회 byte 결정성, policy 교집합/계보, 오류 stdout 0바이트) | T14-4 | green — Jaeger Linux 관통은 T14-5 잔여 |
| FR-ADP-09 (부분) — T8 Codex NDJSON 7건을 픽스처 기반으로 정규화하고, 명시적 auto 외 수동 승인에서는 effect를 fail-closed; unmapped native line은 disposition으로 기록하고 observable 등급은 픽스처 근거 범위로 한정 | `seams/subagent/codex/golden_test.go` (7개 golden·SHA fingerprint·중간 이벤트 유실 0) + `seams/subagent/codex/parse_test.go` (manual 승인 부재·명시 auto·미지 이벤트·중단) | T14-3 | green — Codex 독립 실행 surface 조립 및 Linux 관통은 후속 범위 |
| codegen 산출 타입 ↔ 스키마 정합 (T1 완료 기준 a) | `contracts/validate/validate_test.go` (TestGenTypesRoundTrip) | T1 | green |
| schemagen fail-closed·결정성·병합 규칙 | `tools/schemagen/gen_test.go` | T1 | green |
| codegen drift 게이트 (미추적 신규 파일 포함) | `tools/schemagen/drift_test.go` + `make codegen-drift` (CI 편입) | T1 | green |
| donePayload 거울 정의 동일성 | `contracts/schema_test.go` | T1 | green |
| FR-LOG-06 — 리플레이 결정론 속성 (300회, 입력 불변성 포함) | `core/replay_prop_test.go` — **T4에서 logd.Replay 배선, xfail 태그 제거 후 본 스위트 편입** | T2→T4 | green |
| FR-POL-03 — 병합 협소성 속성 (300회, 체인·교환·자기병합·승인 완화 금지) | `core/policy_prop_test.go` — **T6에서 policy.Merge 배선, xfail 태그 제거 후 본 스위트 편입** | T2→T6 | green |
| FR-POL-02 — 순수 평가 함수 (결정성·워크스페이스 경로 경계·egress 편입/거부 분리·깊이 거부) | `core/policy/policy_test.go` (TestEvaluatePure, TestEvaluateWorkspaceScope, TestEvaluateDepthDenial) | T6 | green |
| FR-POL-06 — 예산 초과 판정 (토큰/시간/깊이, 경계값 포함 7케이스) | `core/policy/policy_test.go` (TestExceededReason) | T6 | green |
| FR-POL-03 — 병합 단위 (교집합 정렬·최솟값·승인 강화·ID 계보) | `core/policy/policy_test.go` (TestMerge, TestIntersectDeterministic) | T6 | green |
| T6 재리뷰 — 워크스페이스 경로 탈출 차단 (POSIX 정규화 후 판정, 빈·상대 경로 거부, 빈 스코프 엔트리 무권한, 정규화 경로 반환) | `core/policy/policy_test.go` (TestEvaluateWorkspaceScope 탈출 3종+evil, TestEvaluateEmptyScopeEntryGrantsNothing, TestEvaluateReturnsNormalizedWorkspace) | T6 | green |
| FR-POL-01 — YAML 프로파일 파서 (strict+중복 키 거부, 다중 문서 거부(2차 Decode==io.EOF), 승인 명시·예산 3축 비음수·fs 절대경로 정규화) | `core/policy/parse_test.go` (거부 12종 + 유효·auto 명시) + boundarylint yaml import 한정 | T6 | green |
| T2 완료 기준 — 생성기가 스키마 유효·다양·결정적 입력을 실제 생성 | `core/propgen_test.go` (TestGenerator*) | T2 | green |
| FR-LOG-01 — append-only 저장소 수준 물리 차단 (직접 연결 UPDATE/DELETE 거부) | `seams/store/sqlite/store_test.go` (TestAppendOnlyTriggers) | T3 | green |
| FR-LOG-02 — 단일 writer seq 전순서 + mutation 비노출 (동시 제출·store 도달 순서, Reader 동적 타입의 Append/Close 미노출, 파일 직접 INSERT는 PK 최후 방어) | `core/logd/writer_test.go` (TestWriterSeqTotalOrder) + `seams/store/sqlite/store_test.go` (TestSingleWriterOverSQLite) + `seams/store/sqlite/bypass_test.go` (외부 패키지 assertion 검사) | T3 | green |
| T3 재리뷰 — contracts 위반 저장 거부, admission-only ctx, terminal 전파(원인 체인 포함), 백프레셔 cap 직접 단정, 특수문자 DSN | `core/logd/writer_test.go` (TestWriterRejectsContractViolations, TestSubmitAckDespiteCtxCancelAfterAdmission, TestWriterTerminalOnStoreFailure) + `seams/store/sqlite/store_test.go` (TestWeirdFilenames, TestLogPathEnforcesRedactionAndContract) | T3 | green |
| FR-LOG-07 — raw 원본 보존 (NULL/빈 base64 구분 왕복) | `seams/store/sqlite/store_test.go` (TestRoundTrip) | T3 | green |
| FR-LOG-08 — 기록 전 redaction (기본 패턴 + 설정 확장, payload·raw) | `core/logd/writer_test.go` (TestWriterRedaction) | T3 | green |
| FR-LOG-09 — 백프레셔 (큐 포화 시 입력 중단→재개, 수락분 전량 저장, 유실 0) | `core/logd/writer_test.go` (TestWriterBackpressure) | T3 | green |
| NFR-02 — 크래시 복구 (SIGKILL helper 후 마지막 ack seq까지 복원) | `seams/store/sqlite/crash_test.go` (TestCrashRecovery) | T3 | green |
| NFR-02/03 — WAL + synchronous=FULL 실적용, 재기동 seq 승계 | `seams/store/sqlite/store_test.go` (TestDurabilityPragmas, TestLastSeqAcrossReopen) | T3 | green |
| busy_timeout↔취소 충돌 회귀 (잠금 상태 50ms deadline 즉시 반환) + BUSY 재시도 | `seams/store/sqlite/store_test.go` (TestBusyReturnsPromptly…, TestBusyRetry…) | T3 | green |
| 외부 모듈 import 한정 (modernc→seams/store/sqlite, jsonschema→contracts/validate) | `tools/boundarylint/rules_test.go` (TestCheckExternalRestrictions) + `make lint` 실그래프 | T3 | green |
| NFR-04 — CGO_ENABLED=0 네이티브 smoke (race와 분리) | `make smoke` (CI 편입) | T3 | green |
| FR-LOG-03/04/10 — 모델 가시 히스토리·usage 프로젝션, 자식 중간 이벤트 제외·subagent/done만 진입, 재계산 가능성 | `core/logd/replay_test.go` (TestReplayMessagesProjection, TestReplayChildSpanConversationExcluded, TestReplayUsageAggregation, TestReplayDoesNotMutateInput) | T4 | green |
| FR-LOG-05 — 포크: 새 trace_id·원본 참조·포크 지점 상태 동일·원본 불변·불량 지점 거부 | `core/logd/replay_test.go` (TestForkPreservesStateAndOrigin, TestForkRejectsBadPoints) + `seams/store/sqlite/fork_test.go` (TestForkAcrossFiles — 실파일 독립 진행) | T4 | green |
| T4 재리뷰 — 포크 목적지 안전(자기 포크·비공백 목적지 거부, writer 루프 내 원자적 공백 확인, 정확한 atSeq 존재), usage 합산 fail-closed(overflow·음수 거부) | `core/logd/replay_test.go` (TestForkDestinationMustBeEmpty, TestForkRequiresExistingSeq, TestReplayUsageOverflowRejected) + `seams/store/sqlite/fork_test.go` (TestForkDestinationSafetyE2E — 공개 API 실파일) | T4 | green |
| T4 재재리뷰 — 포크 배치 저장소 수준 all-or-nothing (AppendBatch 트랜잭션) | `core/logd/replay_test.go` (TestForkAtomicOnStoreFailure) + `seams/store/sqlite/store_test.go` (TestAppendBatchAtomic — 실저장소 rollback) | T4 | green |
| FR-LOOP-02/03/04 — 훅 4지점 고정, 판정 3종, 충돌 해소(reject>rewrite>continue, 복수 rewrite 등록 순서) | `core/loop/hooks_test.go` (TestResolveDecisionsTable — 12조합, TestValidateDecision) + `core/loop/loop_test.go` (TestRegisterHookRejectsUnknownPoint, TestHooksAreIndependent) | T5 | green |
| FR-LOOP-05 — reject된 첫 step은 step 없는 durable turn | `core/loop/loop_test.go` (TestRejectedFirstStepLeavesDurableTurnWithoutSteps) + `seams/store/sqlite/loop_e2e_test.go` (실파일 reopen 후 재생) | T5 | green |
| FR-LOOP-01/06 — 상태 머신 경계·판정·사유의 이벤트 기록, 모델 요청의 로그 재구성(FR-LOG-03 구조 강제) | `core/loop/loop_test.go` (TestTurnFlowWithTools, TestPreToolRewriteApplied, TestPreToolRejectSkipsExecution, TestTurnStoppingRejectForcesAnotherStep, TestMaxStepsGuard, TestInvalidHookDecisionFails) | T5 | green |
| T5 재리뷰 — Writer 결속 프로젝션(혼합 불가), 오류·취소 경로 경계 보존, rewrite 전체 교체(strict decode) | `core/loop/loop_test.go` (TestModelCancellationKeepsBoundaries, TestSchemaViolatingUsageKeepsBoundaries, TestPreToolRewrite* 계열, TestPreStepRewriteIsFullReplacement, TestPostToolRewriteNonObjectFails…) | T5 | green |
| T5 재재리뷰 — hook/verdict durable 기록(훅의 ctx 취소에도 유실 0), 검증 후 기록(부분 기록 방지), 지점별 rewrite 대상 검증 | `core/loop/loop_test.go` (TestHookCancellationKeepsVerdictDurable, TestInvalidLaterDecisionPreventsPartialVerdictRecord, TestInvalidRewriteTargetPreventsVerdictRecord) | T5 | green |
| T5 payload 확정 (2026-08-17 [H] 승인) — 대화·툴 4종 + turn/step 경계 4종 폐쇄, tool/result status 판별, output 객체 정규화, args 정규화 | `contracts/validate/validate_test.go` (확정 유효 7종/위반 8종) + `core/propgen_test.go` (생성기 갱신) + codegen drift 게이트 | T5 | green |
| FR-ADP-01/02/03 — 어댑터 독립 실행 파일(NDJSON stdio), spawn/send/events/stop 최소 계약, ready·done MUST + 중간 이벤트 정규화 | `seams/subagent/subagent_test.go` (TestNullAdapterNormalization, TestStopPath) + `surfaces/hx/e2e_test.go` (실바이너리 관통) | T7 | green |
| §5.2 계약 위반 어댑터 거부 (비 NDJSON, raw 누락, 미지 kind, done 없는 종료) + 발신 명령 자체 검증 | `seams/subagent/subagent_test.go` (TestContractViolatingAdapterRejected) | T7 | green |
| FR-CLI-01/02 — hx run·hx replay(--to), stdout NDJSON/stderr 진단(FR-CLI-06), 재생 결정론·동일 상태 | `surfaces/hx/e2e_test.go` (TestRunReplayEndToEnd — replay 2회 동일, --to prefix) | T7 | green |
| FR-LOG-10 — 자식 중간 이벤트는 child span에만, 부모 히스토리에는 subagent/done 결과만 (E2E) | `surfaces/hx/e2e_test.go` (부모 Messages = subagent_result 1건 단정) | T7 | green |
| T8 준비 — 픽스처 비밀 검사 게이트 fail-closed 3분기 (무검출 0 / 검출 1 / 대상 부재·인자 누락·**실제 grep 실행 오류** 2) | `tools/fixturecheck/script_test.go` (TestSecretCheckGate — 7케이스, PATH 주입 가짜 grep으로 오류 분기 실증) | T8 | green |
| T8 준비 — 픽스처 매니페스트 게이트 (README 존재, NDJSON↔meta 대응, meta-only skip 계수 제외, 최소 15건) | `tools/fixturecheck/script_test.go` (TestManifestGate — 6그룹, `tools/check-fixture-manifest.sh` 실행) | T8 | green |
| FR-ADP-05 전제 — Claude Code·Codex 실출력 픽스처 15건(정상/툴 다수/승인 거부/오류/중단), meta-only skip 1건 | `contracts/fixtures/README.md` + `tools/check-fixture-secrets.sh contracts/fixtures` + `tools/check-fixture-manifest.sh contracts/fixtures 15` | T8 | green |
| T7 재리뷰 — §5.2 시퀀스 강제(첫 이벤트 ready, ready 중복·done 이후 출력·ready 전 이벤트 거부 + kill) | `seams/subagent/subagent_test.go` (TestSequenceViolationsRejected — 4형태) | T7 | green |
| T7 재리뷰 — 프로세스 수명 주기(단일 reap, Wait(ctx) deadline 준수, 위반 시 kill·drain, exit 오류 보존) | `seams/subagent/subagent_test.go` (TestWaitHonorsContextAfterDone, TestInvalidEventKillsLingeringProcess, TestAbnormalExitAfterDonePreserved) | T7 | green |
| T7 재리뷰 — hx run 원자적 세션 초기화(InitBatch, 두 번째 run 거부·로그 불변), replay 검증-후-출력(손상 로그 stdout 0바이트) | `surfaces/hx/e2e_test.go` (TestRunRefusesExistingSession, TestReplayCorruptedLogEmitsNothing) | T7 | green |
| T7 재재리뷰 — 종료 관측의 EOF 비종속(reaper + drain 유예), Spawn ctx 취소의 그룹 kill, 공백 줄 §5.2 위반 | `seams/subagent/subagent_test.go` (TestExitObservationIndependentOfStdoutEOF, TestSpawnContextCancelKillsGroup, TestBlankLinesAreViolations — 3위치) | T7 | green |
| T7 재재재리뷰 — exec.Cmd 자원 소유(watchCtx·파이프 누수 소멸: 완료 후 파이프 closure, 반복 spawn goroutine 비증가, 실패 경로 정리), 리더 종료 시 그룹 즉시 종료 + 진짜 EOF drain(유예 fail-open 소멸) | `seams/subagent/subagent_test.go` (TestPipesClosedAfterCompletion, TestNoGoroutineAccumulationAcrossSpawns, TestSpawnSendFailureCleansUp, TestLeaderExitTerminatesRemainingGroup) | T7 | green |
| FR-ADP-02 — Claude Code command 수신(task/stop), `message`는 단발 `-p`·세션 저장 배제에 따른 명시적 fail-closed 제약(done/error 후 종료) | `seams/subagent/claudecode/adapter_test.go` (TestAdapterStopKillsClaudeAndSynthesizesStopped, TestAdapterEmitsDistinctDoneForFailuresAfterReady/message_unsupported) + `docs/t9-adapter-contract-proposal.md` §7.5 | T9 | green |
| FR-ADP-03 — Claude Code 독립 어댑터의 ready/done MUST, init-first, stop·오류 경로 terminal 보존 | `seams/subagent/claudecode/adapter_test.go` (TestAdapterExecutableReplaysAllClaudeFixtures, TestAdapterRejectsNativeEventBeforeInitWithoutBrokenOutput, TestAdapterEmitsDistinctDoneForFailuresAfterReady, TestAdapterStopKillsClaudeAndSynthesizesStopped) | T9 | green |
| FR-ADP-04 — Claude native NDJSON 및 PreToolUse 훅 원본 raw 보존(1원본→N이벤트 동일 raw 포함) | `seams/subagent/claudecode/parse_fail_test.go` (TestSameRawAttachedToEachEvent) + `seams/subagent/claudecode/adapter_test.go` (TestApprovalHandshakeAllowAndDenyPreservesRaw) | T9 | green |
| FR-ADP-05 — Claude stream-json 정규화·픽스처 스냅샷, 중간 이벤트 유실 0 | `seams/subagent/claudecode/golden_test.go` (TestGoldenClaudeFixtures, TestNoIntermediateEventLoss) + `seams/subagent/claudecode/adapter_test.go` (TestAdapterExecutableReplaysAllClaudeFixtures) | T9 | green |
| FR-ADP-06 — Claude 관측 가능 등급 `observable` | `seams/subagent/claudecode/golden_test.go` (8개 골든의 ready.grade) + `seams/subagent/claudecode/adapter_test.go` (TestAdapterExecutableReplaysAllClaudeFixtures) | T9 | green |
| FR-ADP-07 — result.usage 1회 보고, 입력 3항 checked addition, 부재 폴백·손상 fail-closed | `seams/subagent/claudecode/parse_fail_test.go` (TestUsageAbsentIsOmittedNotError, TestCacheFieldsDefaultToZero, TestFailClosedCases의 usage 3종) + 골든 8건 | T9 | green |
| FR-POL-05 — 승인 handshake(명시 auto만 자동 허용, manual nil 기본 deny, durable policy/decision 후 응답, pump 비블로킹, 미상관·중복·빈 reason·기록 실패·stop·취소 fail-closed) | `seams/subagent/subagent_test.go` (TestApprovalPolicyModesAndDurableAttribution, TestApprovalDecisionDoesNotBlockPump, TestApprovalEmptyDenyReasonIsFatalAfterForcedDeny, TestApprovalRecordFailureSendsDenyBeforeTermination) + `seams/subagent/claudecode/adapter_test.go` (TestApprovalHandshakeAllowAndDenyPreservesRaw, TestApprovalResponseCorrelationViolations, TestStopDeniesPendingHookBeforeNativeTerminationKillFirst, TestStopDeniesPendingHookBeforeNativeTerminationOutputFirst, TestContextCancellationCleansPendingHookViaProcessDone) | T9 | green |
| FR-POL-05 실증 — 실 claude 세션에서 훅 발화·부모 판정이 툴 실행을 게이트 (deny=미생성 / allow=생성) | `seams/subagent/claudecode/smoke_test.go` (TestSmokeApprovalHandshake, `-tags smoke`, [H] 사람 실행) + `docs/t9-smoke-result.md` | T9 | green (2026-08-19, claude 2.1.235) |
| FR-POL-05 격리 — 고정 플래그 `--setting-sources project,local`이 사용자 설정을 실제로 배제(대조군 대비) | `seams/subagent/claudecode/smoke_test.go` (TestSmokeUserSettingIsolation, `-tags smoke`; 자격증명·API 호출 없음) + `docs/t9-smoke-result.md` §확인점 1 | T9 | green (2026-08-19, claude 2.1.235) |
| FR-SBX-06 (부분) — `subagent/spawn` payload를 `world_backend` 판별 2분기로 폐쇄, `local-podman` 분기에 profile_id·SHA-256 image_digest·mounts 필수 (SCP-T10-001) | `contracts/validate/validate_test.go` (유효 2분기 + 분기 혼입·tag digest·형식 오류·필수 누락 위반 회귀, TestValidateEventRejectsSpawnKindNotPayload 교차 단정) | T10-1 | green |
| FR-SBX-06 — production world surface가 `world_backend:"none"`을 activation 전에 거부하고, local-podman metadata를 durable spawn ACK 뒤에만 실행 | `surfaces/hx/world_test.go` (TestProductionWorldRejectsNoneWithoutActivation) + `world_integration_test.go` (spawn commit timestamp < container start timestamp) | T10-final | green — Linux run 32640081056 |
| FR-SBX-05 (부분) — fs·subprocess·sandbox를 단일 `world.Backend`/staged lease lifecycle로 소유, 병합 완료 정책의 불변 스냅샷 전달 | `core/world/world_test.go` (TestEffectivePolicyAndSpawnSpecAreSnapshots, TestFakeActiveLeaseLifecycleOrderEffectsAndErrors) | T10-2,6 | green |
| FR-SBX-05 — surface가 backend-neutral `Prepare → CommitSpawn → Activate`와 host worldadapter의 process/approval descriptor만 사용해 실제 local Podman lease를 관통 | `surfaces/hx/world.go` + `world_integration_test.go` (normal/abnormal/stop/orphan 실물 lifecycle) | T10-final | green — Linux run 32640081056 |
| FR-ADP-10 (부분) — process/approval broker endpoint와 overlay upper를 host-only lease 경계로 분리 | `core/world/world_test.go` (TestUpperDirExistsOnlyOnHostLeaseBoundary, TestFakeActiveLeaseLifecycleOrderEffectsAndErrors, TestProcessAndApprovalEndpointsAreNominalAndDescriptorIsHostOnly) | T10-2,6 | green |
| FR-ADP-10 — 실제 container agent는 PID 1이며 host adapter binary·process/approval host socket·Podman socket이 mount되지 않고 adapter hash가 실행 전후 불변 | `surfaces/hx/world_integration_test.go` (assertContainerIsolation, adapter SHA-256 before/after) | T10-final | green — Linux run 32640081056 |
| T10 Q7 — `core/world/worldtest` Fake의 production import 금지, `_test.go`에서만 허용 | `tools/boundarylint/rules_test.go` (TestCheckWorldtestProductionImport) + `tools/boundarylint/integration_test.go` (실제 go list production/test import 분리) + `make lint` 실그래프 | T10-2 | green |
| T10-2 리뷰 회귀 — Fake lifecycle 단계 미도달이 무한 대기가 아니라 2초 내 명시적 timeout으로 실패 | `core/world/world_test.go` (waitSignal, waitError가 모든 lifecycle channel 수신을 상한 처리) | T10-3 | green |
| FR-SBX-01 (부분) — Linux 전용 rootless Podman backend, rootless/native-overlay preflight, inspect로 digest·image numeric UID/GID 확정, container create/start/stop/wait/remove | `seams/world/local/local_test.go` (TestOpenBuildsRootlessOverlayAndMetadata, TestBackendAndPodmanPreconditionsFailClosed, TestLeaseCloseWaitsForEffectsThenCleansContainerAndPreservesUpper, TestCloseContinuesAfterStopError) — command runner 단위 테스트이며 실물 Podman 근거 아님; network 구성은 T10-4 행에서 별도 추적 | T10-3 | green |
| FR-SBX-01 — rootless/native-overlay Podman에서 FROM scratch testagent를 repository@digest로 실행하고 normal·abnormal·stop·orphan lifecycle을 bounded 정리 | `make world-integration` → `surfaces/hx/world_integration_test.go` | T10-final | green — Linux run 32640081056 |
| FR-SBX-02 (부분) — workspace·scope 실경로 재검사, lower 밖 0700 state root, custom `:O,upperdir,workdir`, same-device 검사, `:U` 금지, collector 전 upper 보존 | `seams/world/local/local_test.go` (TestOpenBuildsRootlessOverlayAndMetadata, TestOpenFailsClosedBeforeContainerStart, TestCreateAndStartFailuresCleanResources) + `core/world/world_test.go` (정책/scope 방어 복사) — 명령·경로 단위 검증 | T10-3 | green |
| FR-SBX-02 — 실제 native overlay lower 불변, upper create/modify, 삭제의 subuid-owned char-device whiteout을 owner filter 없이 lstat(mode+rdev)로 확인; work는 exact-path capability로 userns 정리하고 upper는 collector ACK 전 보존 | `seams/world/local/overlay_cleanup_test.go` (경로·Podman·잔존 오류 구분, symlink 탈출, exact argv) + `surfaces/hx/world_integration_test.go` (assertLowerAndUpper 및 lifecycle별 runtime 잔존 0) | T10-final | green — cleanup probe 32590510041, Linux gate 32640081056 |
| FR-SBX-06 (부분) — local backend가 inspect 확정 digest·profile·resolved lower·relative upper_ref를 SpawnMetadata로 산출 | `seams/world/local/local_test.go` (TestOpenBuildsRootlessOverlayAndMetadata); durable spawn 선기록은 surface 조립 전이므로 아직 완료 아님 | T10-3 | green |
| FR-SBX-03 (부분) — agent는 internal network만, proxy sidecar만 internal+external network를 받고 audit socket은 sidecar에만 mount; HTTP_PROXY 제거 시 권한이 넓어지지 않는 명령 구성 | `seams/world/local/local_test.go` (TestOpenBuildsRootlessOverlayAndMetadata, TestLeaseCloseWaitsForEffectsThenCleansContainerAndPreservesUpper, TestCreateAndStartFailuresCleanResources) — Fake Podman 명령/lifecycle 단위 검증이며 실물 network 격리 근거 아님 | T10-4 | green |
| FR-SBX-03 (부분) — stdlib HTTP/HTTPS CONNECT proxy, domain case-fold·trailing-dot·label-boundary suffix, IP literal·임의 CONNECT port 거부, DNS private/loopback/link-local/metadata·혼합 rebinding 차단, resolve 결과로 pinned dial | `seams/world/local/egressproxy/proxy_test.go` (TestAllowlistExactAndLabelBoundary, TestDeniedTargetsNeverDialAndAreAudited, TestHTTPForwardingUsesPinnedResolvedAddress, TestHTTPSConnectTunnel, TestConnectOnlyAllowsDomainPort443) | T10-4 | green |
| FR-EXT-02/04 (부분) — `local-podman` spawn을 extension 없음/있음 폐쇄 분기로 나누고, 확장별 고정 version·sha256 integrity·source·artifact_digest 및 결정적 순서·중복 금지를 검증; 비밀·호스트 절대경로 기록을 금지 | `contracts/validate/validate_test.go` (확장 유효 2종·필수/형식/분기 혼입/중복/순서/미지 필드 회귀) + `tools/schemagen/gen_test.go` (구조적 중복 discriminator 회귀) + codegen drift | T13-1 | green — 선언·프로비저닝·정책 병합·캐시·실물 게이트는 T13 잔여 |
| FR-EXT-01/02/05/06, FR-POL-03 — 확장 선언의 exact version·sha256 integrity·registry canonicalization을 검증하고, `allowed_extensions`·`allowed_registries`를 교집합으로 병합(생략은 빈 집합), 요청 확장 밖이면 부작용 없이 deny하며 명시된 실행 egress만 정책 교집합으로 편입 | `core/policy/policy_test.go`, `core/policy/parse_test.go`, `core/policy_prop_test.go` (300회 병합 속성) | T13-2 | green — 프로비저너·컨테이너·콘텐츠 주소 캐시·Linux 게이트는 T13 잔여 |
| FR-EXT-03/04/08, §8-8 — 실제 로컬 HTTP artifact service와 rootless Podman provisioning container를 registry-only profile로 관통하고, digest mismatch·policy deny·runtime registry deny·egress allow·cache 재검증 및 확장 spawn metadata를 확인 | `surfaces/hx/extensions_integration_test.go`, `make extensions-integration` (Linux CI; 7 시나리오) | T13-4 | Linux CI 5회 연속 green 필요 — 동일 SHA와 run/job ID를 PR에 기록 |
| FR-EXT-03/08 (부분) — registry-only provisioning profile과 one-shot runtime seam, digest 재검증 CAS cache, 원자적 read-only bundle 봉인 및 실행 world의 bundle read-only mount | `seams/world/local/contentcache_test.go` (cache hit 재검증·손상 fail-closed·partial bundle 제거) + `core/world/world.go`/`seams/world/local/local.go` (host-only bundle capability·read-only mount) | T13-3 | green — 실제 rootless Podman provisioning/registry 및 Linux 관통 게이트는 T13-4 잔여 |
| FR-SBX-03 — rootless internal bridge에서 IP literal 직결 실패, forbidden domain deny audit, allow HTTP·CONNECT audit 및 body/header/credential 비기록 | `surfaces/hx/world_integration_test.go` (assertEgressEffects) + container `testagent` 실제 요청 | T10-final | green — Linux run 32640081056 |
| FR-COL-03 준비 — allow·deny EgressAttempt 메타데이터(domain/method/request bytes/time/decision/reason), audit-before-dial, bounded queue 포화·enqueue 실패 fail-closed, proxy stop 뒤 stream drain/ACK 전 cleanup 금지 | `seams/world/local/egressproxy/proxy_test.go` (TestAuditFailurePreventsDial, TestAuditHappensBeforeDialAndCarriesMetadataOnly) + `seams/world/local/audit_test.go` (TestAuditBrokerBackpressureAndDrainGate, TestAuditBrokerRejectsMalformedAttemptWithoutEnqueue) + `seams/world/local/local_test.go` (TestLeaseCloseWaitsForEffectsThenCleansContainerAndPreservesUpper) | T10-4 | green |
| SCP-T11-001 / FR-COL-03 — `collector/egress`를 프록시 관측 allow/deny 시도 판정 분기로 폐쇄하고 deny 사유(비어 있지 않음·최대 512자, 민감정보 기록 금지)를 강제 | `contracts/validate/validate_test.go` (allow/deny 유효 샘플 + reason·decision·분기 혼입·길이 위반) + `contracts/gen/events.gen.go` (codegen) + `core/propgen_test.go` + `core/logd/replay_test.go` | T11-1 | green |
| FR-COL-02/03/05 — collector 라이브러리가 spawn lower baseline과 upper fs diff 및 egress record 후보를 생성하고, 결정적 정렬·whiteout/opaque 처리·자원 상한을 fail-closed로 강제 | `collector/collector_test.go` (baseline/diff 결정성·lower mutation·opaque 삭제·상한·egress payload/record) + Linux 전용 `collector/whiteout_linux_test.go` (소유자 무관 char-device/rdev 술어); Linux overlay probe `33049557686` (opaque xattr·directory replacement·child whiteout) | T11-2 | green (surface 조립·실물 관통은 T11-3 잔여) |
| FR-COL-01/02/03/05/06 — hx surface가 world lease와 collector·writer를 조립해 baseline→spawn ACK→activate→종료 scan→fs_changed ACK→upper cleanup 순서를 보장하고, effect는 writer durable ACK 뒤 proxy ACK로 되돌림 | `surfaces/hx/world.go`, `core/world/world.go`, `seams/world/local/local.go`; `surfaces/hx/world_integration_test.go` (실물 overlay 변경·opaque/replacement/whiteout·allow/deny egress·abnormal lifecycle·상한/cleanup assertions) | T11-3 | green — Linux 관통 게이트 33096358708, 동일 SHA 시도 6–10 전부 성공 |
| FR-COL-03 잔여 — EgressAttempt를 `collector/egress` durable 이벤트로 변환 | T11-3에서 surface가 writer ACK를 effect/proxy ACK 전에 대기하도록 배선 완료 | T11-3 | green — 위 관통 게이트 및 단위 회귀 |
| FR-AUD-01/02 부분 — contracts 이벤트에서 의도·효과 행을 추출하고 canonical 상대 POSIX 경로·child span·seq 기반 one-to-one 대조(정규화 실패·중복·불완전 관측 fail-closed) | `core/audit/audit_test.go` (canonical path 경계, cross-span, 다중 경로 결정성, collector 부재, ambiguous duplicate, `changes:[]` 불완전성) | T12-1 | green — CLI·surface·Linux 관통 대조는 T12 잔여 |
| FR-AUD-01/02 — report value type과 결정적 renderer가 classification/span/parent/path/change/seq/reason 행 및 요약을 byte-identical하게 출력하고, 불완전 관측은 성공 표 없이 오류로 종료; audit의 collector/logd/seams/surfaces import를 boundarylint로 금지 | `core/audit/audit_test.go` (다중 경로 100회 결정성·요약 정합성·incomplete 출력 차단·parent span) + `tools/boundarylint/rules_test.go` (TestAuditImportDirection) | T12-2 | green — CLI·surface·Linux 관통 대조는 T12 잔여 |
| FR-AUD-03/04, FR-CLI-04 — `surfaces/hx`가 SQLite Reader snapshot 하나를 audit decoder와 `logd.Replay`에 공유하고 `hx audit`의 span/actor/cost/`--at-seq` 질의, 부모 가시 컨텍스트 및 stdout/stderr fail-closed를 조립; span 비용도 Replay의 checked projection에서 조회 | `surfaces/hx/audit_test.go` (prefix 경계·span cost/context 동일 snapshot, 불완전 stdout 0바이트, 무매칭 필터 오류) + `core/logd/replay_test.go` (span·actor usage projection) + `surfaces/hx/main.go` (`auditSession`, `readTo` 공용 prefix) | T12-3 | green — Linux 관통 대조는 T12-4 잔여 |
| FR-AUD-01/02, FR-AUD-03/04, FR-CLI-04 — 실제 T11 Linux container durable log에서 보고된 `approved-marker.txt`는 `matched`, hidden create/modify/delete는 `observed_unreported`, 무효 보고는 `reported_unobserved`로 대조하고 child span·경로 정규화를 검증 | `surfaces/hx/world_integration_test.go` (`assertAuditClassification`) + 실제 container `testagent`의 `reported-only.txt` 무효 intent | T12-4 | 5회 연속 green 필요 — 동일 SHA Linux run ID는 PR에 기록 |
| FR-ADP-10, FR-POL-05 (부분) — agent에는 per-lease request-only relay만 노출하고 host adapter endpoint/capability는 비노출; native `(call_id,name,canonical args)` one-shot 상관 뒤에만 승인 진행, hook-first는 intent까지 allow 없이 대기; mismatch·duplicate·timeout·lease 종료는 강제 durable deny이며 Decider 재호출 없음 | `seams/world/local/approval_test.go` (TestApprovalRelayMatchesOneShotAndAuditsDuplicate, TestApprovalHookBeforeIntentWaitsWithoutAllowAndThenMatches, TestApprovalRelayMismatchAndTimeoutAreForcedDenyDeliveries, TestApprovalAdapterCapabilityAndSpanAreLeaseBound) + `seams/subagent/claudecode/approval_world_test.go` (TestWorldApprovalClientRegistersIntentAndPreservesForcedHook, TestAdapterExecutableRegistersRealFixtureToolIntentWithWorldBroker) + `seams/subagent/subagent_test.go` (TestApprovalDuplicateRaceIsDurablyReconstructableWithoutSecondDecision, TestApprovalRelayForcedDenialsNeverReachDecider, TestApprovalDeadlineDeniesAndRecordsInsteadOfWaitingForever, TestApprovalExpiredExplicitAutoModeStillDenies) — Unix socket/Fake Podman 단위 검증이며 실제 container 경계 근거 아님 | T10-5 | green |
| FR-ADP-10, FR-POL-05 고갈 방어 (부분) — relay line/pending/request-rate, intent ledger, delivery queue가 bounded이고 포화·scope 불일치는 평범한 deny로 숨지 않고 broker fatal; hook은 fail-closed deny, adapter/Decider 전달 0; adapter 부재 shutdown도 deadline 안에 종료 | `seams/world/local/approval_test.go` (TestApprovalRelayResourceExhaustionIsFatalAndVisible, TestApprovalRelayOversizeIsFatalBeforeAdapterDelivery, TestApprovalRelayProtocolCannotInjectAdapterOperations, TestApprovalBrokerShutdownHasDeadlineWithoutAdapter) + `seams/subagent/claudecode/approval_world_test.go` (TestWorldApprovalClientCloseUnblocksPendingDecision, TestConfigFromEnvConsumesWorldCapabilityWithoutPassingItToAgent) | T10-5 | green |
| FR-ADP-10, FR-POL-05 — 실제 relay에서 allow 뒤 marker 1건, duplicate는 새 request_id·forced deny·Decider 재호출 0·native rejected로 재구성 | `surfaces/hx/world_integration_test.go` (assertApprovalAndFlood) + container `testagent` exact hook raw | T10-final | green — Linux run 32640081056 |
| FR-SBX-05, FR-ADP-10 — `ProcessEndpoint`/`ApprovalEndpoint` 명목 분리, approvalwire/processwire 패키지·socket·capability 분리, seam 간 구현 import 금지 | `core/world/world_test.go` (TestProcessAndApprovalEndpointsAreNominalAndDescriptorIsHostOnly) + `tools/boundarylint/rules_test.go` (TestWorldEndpointImplementationImportDirection) + `seams/world/local/local_test.go` (TestOpenBuildsRootlessOverlayAndMetadata) | T10-6 process broker | green |
| FR-SBX-06 — `Prepare → CommitSpawn(writer durable ACK) → Activate` staged lease와 metadata/lease/seq 결속 비공개 receipt; zero·cross·reuse·기록 실패는 network/container create 0 | `core/world/world_test.go` (TestCommitSpawnMintsLeaseBoundReceiptOnlyAfterDurableAck, TestCommitSpawnRejectsMismatchBeforeWriter) + `seams/world/local/local_test.go` (TestPrepareCreatesNoRuntimeObjectsAndAbortCleansState, TestReceiptFailuresDoNotCreatePodmanResources, TestSpawnCommitFailureLeavesContainerCreateAtZero, TestSpawnDurableAckPrecedesRuntimeCreation) | T10-6 process broker | green |
| FR-SBX-05, FR-LOG-09 — control/output 2연결 framed v1과 실제 output socket→writer gate→container write backpressure, 해제 뒤 이벤트 유실 0 | 단위: `core/world/processwire/wire_test.go`, `process_broker_test.go`; 실물: `surfaces/hx/world_integration_test.go` flood 2,048건 + 완료 marker gate | T10-final | green — Linux run 32640081056 |
| FR-SBX-01, FR-ADP-10 — 단일 `podman wait --condition exited` 종료 권위, start-attach transport 분리, exit_observed/stream_end 독립, stop→kill→wait와 orphan descendant 정리 | 단위: `seams/world/local/process_broker_test.go`, `command_process_test.go`; 실물: `surfaces/hx/world_integration_test.go` lifecycle subtests | T10-final | green — Linux run 32640081056 |
| FR-SBX-01 immutable image lookup — trusted repository와 digest를 분리하고 inspect/create 모두 `repository@sha256:…`, durable SpawnMetadata에는 digest만 기록 | `seams/world/local/local_test.go` (TestOpenBuildsRootlessOverlayAndMetadata, TestBackendAndPodmanPreconditionsFailClosed) + `surfaces/hx/world_integration_test.go` (FROM scratch image build/inspect/run) | T10-final | green — probes 32577032377/32577141727, Linux gate 32640081056 |
| FR-LOG-09 / T10 lifecycle — 단일 `podman wait` 종료 권위, stopped attach의 bounded drain/reap, consumer-gone-after-done 분류, ExitObserved↔Stop ACK ordering 보존 | `seams/world/local/process_broker_test.go`, `seams/subagent/worldadapter/process_client_test.go` + `surfaces/hx/world_integration_test.go` (`lifecycle-stop`, `lifecycle-stop-ignore`) | T10 lifecycle amendment | green — Linux run 32934687022 attempts 1–5 (동일 SHA 62aa0ad) |
| FR-SBX-01 (잔여, Claude Code 실물 경로) — 실제 Claude image/container 실행과 실 토큰 세션은 아직 관통되지 않음; T10 testagent 근거로 대체하지 않음 | T15-5 CI + [H] macOS VM smoke | T15 | 미착수 |
| FR-SBX-01, FR-ADP-10 (부분) — `claudecode`가 `local-podman`의 host-only `ProcessEndpoint`로 container native stdio를 중계하고, `world_backend:none`은 기존 host `procgroup` 경로를 보존; 승인 hook은 기존 per-lease relay를 그대로 사용 | `seams/subagent/claudecode/adapter_test.go` (ProcessEndpoint 선택이 host procgroup으로 폴백하지 않음), `seams/subagent/worldadapter/process_client_test.go` (framed process client lifecycle), `seams/subagent/claudecode/approval_world_test.go` (relay endpoint env 분리) | T15-3 | green — 실제 Claude image/container·토큰·Linux 관통은 T15 잔여 |
| FR-SBX-04 (scope 잔여) — 스코프가 축소된 단기 자격증명 | access token 발급자가 계정 전체 권한만 제공하여 scope 축은 미충족. 명세 하향·API key/refresh fallback 없이 [H] 후속 판단으로 유지 | T15 | 미착수 |
| FR-SBX-04 (부분) — 단기 OAuth access token을 host-only secret capability로 보유하고 argv·wire·metadata·오류에 노출하지 않으며, spawn 예산+clock-skew 전 만료 deny 및 실행 중 `done(error, "token expired")` | `core/world/world_test.go` (capability 비직렬화·redaction), `seams/world/local/local_test.go` (환경 1회 주입·argv 부재·만료 부작용 0), `seams/world/local/process_broker_test.go`·`seams/subagent/claudecode/adapter_test.go` (frame redaction·만료 done) | T15-4 | green — access token 계정 전체 scope 미충족 및 실 토큰 smoke는 T15-5/[H] 잔여 |
| FR-SBX-01/FR-ADP-10 (부분) — pinned Claude 2.1.252를 실제 rootless Podman world에 넣고 ProcessEndpoint·relay·overlay·egress 경계를 무자격증명으로 관통하며 tokenless 인증 실패를 `done{error}`로 관측; 기존 T10 world gate가 순서·lifecycle·overlay·egress·relay 1~5를 함께 검증 | `surfaces/hx/t15_integration_test.go`, `make t15-integration`, `.github/workflows/ci.yml` `t15-linux-gate` 5-attempt matrix | T15-5 | Linux CI run 33894797227에서 동일 SHA 5회 green (jobs 101094805646, 101094805714, 101094805675, 101094805727, 101094805587; 일반 CI 101094805305) — 실 토큰/Claude tool call은 [H] 잔여 |
| FR-SBX-04 (부분) — 실 access token 주입·redaction·만료 음성 대조 절차를 사람 전용으로 고정; token scope 축소는 벤더 미지원으로 여전히 미충족 | `docs/t15-smoke-runbook.md` (Podman VM, 비기록 주입, 확인점 (a)~(d), 만료 음성 대조) | T15-5/[H] | 실 세션 미실행 — 명세 소유자 판정 필요 |
| FR-SBX-01/FR-ADP-10/FR-SBX-04 (사람 smoke 하네스) — macOS Podman machine에서 실제 local-podman world와 Claude adapter를 실행하고, 인증·relay 승인·overlay/egress·redaction/만료 음성 대조를 기계적으로 단정; token scope 축은 여전히 미충족 | `surfaces/hx/t15_smoke_test.go` (`t15smoke` 태그, [H] 전용) + `seams/world/local/t15smoke_export.go` + `docs/t15-smoke-runbook.md` | T15 후속 | 컴파일·vet만 확인 — 실 토큰 실행은 [H] 미수행 |
| FR-OBS-01/02/03 — 고정 Jaeger all-in-one 수신기에 실제 OTLP/HTTP export를 보내고 trace ID·부모-자식 트리·adapter/version/profile/status/usage 속성과 동일 snapshot 재-export 결정성을 Linux에서 관통 검증 | `surfaces/hx/jaeger_integration_test.go`, `make otel-integration` (pull/readiness 실패는 인프라 오류로 표면화, skip 금지) | T14-5 | 동일 SHA `7b8f590a3c987ed81d1be90cafa87f77130376b9`에서 5회 green (run `33789276043`, attempts/jobs: 1/`100761624479`, 2/`100762218894`, 3/`100763677990`, 4/`100764410617`, 5/`100765149207`) |
