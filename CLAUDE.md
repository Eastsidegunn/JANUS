# HX 작업 지침

이 저장소는 HX(멀티 에이전트 실행 기판)를 구현한다. 기능 명세는 docs/hx-기능명세서-v0.1.md가 유일한 진실이며, 모든 작업은 명세의 FR/NFR ID에 귀속된다.

## 작업 방식

- 태스크는 TASKS.md에서 한 번에 하나씩 수행한다. 다음 태스크를 선제 구현하지 않는다.
- 모든 커밋 메시지와 PR 설명에 대상 FR ID를 명시한다. 예: `feat(logd): FR-LOG-02 단일 writer seq 발급`
- 완료의 정의는 단 하나다: 해당 태스크의 완료 기준 테스트가 CI에서 green. "구현했습니다"는 완료가 아니다.
- 막히면 구현을 우회하지 말고 BLOCKED.md에 사유를 기록하고 멈춘다.

## 불변식 (위반 시 어떤 코드도 머지 불가)

- 로그는 append-only다. UPDATE/DELETE 경로를 만들지 않는다.
- 로그 쓰기는 writer 하나만 경유한다. 다른 코드가 DB 파일을 직접 열지 않는다.
- 모든 파생 상태는 이벤트 로그에서 재계산 가능해야 한다.
- 정책 병합은 좁아지기만 한다(allow 교집합, 예산 최솟값).
- 의존 방향: contracts ← core ← seams ← surfaces. seam 간 수평 import 금지. collector는 core와 코드 경로를 공유하지 않는다.
- contracts의 스키마가 진실이고 타입은 codegen 산출물이다. 생성된 타입을 손으로 수정하지 않는다.

## 금지 행동

- 테스트를 통과시키기 위해 테스트를 약화·삭제·skip 처리하는 것. 속성 테스트의 반복 횟수 축소 포함.
- 명세에 없는 기능의 선제 구현 (비목표 §1.3 참조: 웹 UI, 규칙 DSL, 자체 마켓플레이스 등).
- 승인 없는 신규 외부 의존성 추가. 필요하면 BLOCKED.md에 후보와 사유를 적고 멈춘다.
- contracts/ 하위 파일의 수정. 스키마 변경이 필요하다고 판단되면 구현하지 말고 변경 제안만 기록한다.
- fixtures/ 하위 녹화 파일의 수정. 픽스처는 외부 도구 출력의 스냅샷이므로 손대지 않는다.
- 목업·스텁을 실제 구현인 것처럼 두는 것. 스텁이 필요하면 이름에 Null 또는 Fake 접두사를 붙이고 테스트 전용 경로에 둔다.

## 빌드·검증 명령

```
make lint        # 경계 린트 포함 — 실패 시 구조 위반
make test        # 단위 + 속성 테스트
make fixtures    # 어댑터 골든 픽스처 대조
make ci          # 위 전부. PR 전 필수
```

## 검증 우선순위

작업 완료 주장 전에 반드시 순서대로:
1. `make ci` 전체 green 확인
2. 자신의 변경이 어느 불변식과 접촉하는지 한 문장으로 서술
3. 접촉하는 불변식의 속성 테스트가 실제로 해당 경로를 커버하는지 확인. 커버하지 않으면 테스트를 먼저 보강한다.

## 참고 문서

- 기능 명세: docs/hx-기능명세서-v0.1.md
- 작업 큐: TASKS.md
- 추적 매트릭스: docs/traceability.md (FR ID → 테스트 매핑)
