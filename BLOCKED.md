# BLOCKED

구현을 우회하지 않고 멈춘 지점의 기록 (CLAUDE.md 작업 방식).
해소되면 해당 항목을 지우고 태스크를 재개한다.

---

## T10 — 차단 (2026-08-20 확인, 착수 정지)

**사유**: 이 머신에 컨테이너 런타임이 없다. 2026-08-20 재확인 —
`docker` `podman` `nerdctl` `lima` `colima` `finch` 전부 미설치,
`/var/run/docker.sock` 없음. FR-SBX-01이 OCI 컨테이너 실행을 MUST로
요구하므로 완료 기준(§8-4 egress 차단 통합 테스트)을 만들 수 없다.
런타임 도입은 신규 외부 의존성이므로 사람의 승인이 필요하다.

**착수 전에 정해야 할 것 — 런타임보다 먼저 실행 환경이다.**

이 개발 머신은 macOS(darwin/arm64)다. macOS에는 리눅스 컨테이너가 없고,
어떤 도구를 깔든 실제로는 **Linux VM 안에서** 돈다. 그래서 FR-SBX-02의
overlayfs도 VM 내부 리눅스 커널에서 성립하는 것이지 호스트에서가 아니다.
즉 macOS에서의 "local 백엔드"는 리눅스에서의 그것과 의미가 다르며,
여기서 통과한 통합 테스트가 배포 대상 환경을 대표한다고 말할 수 없다.

이 사실이 선택지의 우열을 바꾼다. 아래 C가 우회가 아니라 오히려 더 충실한
검증일 수 있다.

| 안 | 내용 | 대가 |
|---|---|---|
| A | podman(rootless) 로컬 설치 — `podman machine`으로 Linux VM 기동 | 신규 의존성 + VM 디스크. 검증 환경이 VM 경유라 대표성이 약하다 |
| B | colima/docker 로컬 설치 | A와 같은 VM 구조. rootless 기본(FR-SBX-01 SHOULD)은 A가 유리 |
| C | 통합 테스트를 Linux CI에서만 실행, 로컬은 Fake 백엔드 단위 테스트 | 로컬에서 직접 못 돌린다. 대신 실제 리눅스 커널에서 검증된다 |

C를 택하면 CI 러너에 런타임이 실제로 있는지 먼저 확인해야 한다(ubuntu
러너의 docker는 확실하나 podman은 확인 필요). A와 C는 배타적이지 않다 —
C를 진실의 기준으로 두고 A를 개발 편의로 얹을 수 있다.

**T9에서 배운 것을 여기에 적용한다.** T9의 smoke는 자격증명 때문에 CI에
못 넣었고, 그 결과 승인 경로 회귀가 자동으로 안 잡히는 구멍이 남았다.
T10은 반대다 — 자격증명이 필요 없으므로 **CI에 넣을 수 있다.** 넣을 수
있는 것을 로컬 전용으로 만들면 같은 구멍을 자초하는 것이다.

**함께 걸리는 설계 결정 (런타임 선택에 영향)**: FR-ADP-10에 따라 어댑터는
호스트, 에이전트는 컨테이너다. 그러면 T9에서 만든 승인 훅(`hxapprove`)이
컨테이너 안에서 돌고 부모는 밖에 있으므로, Unix 소켓이 경계를 넘어야 한다.
bind mount로 풀지, 다른 전송으로 바꿀지는 T10 착수 시 제안서에서 정한다.
런타임마다 소켓 bind mount의 제약이 다르므로 선택 전에 알고 있어야 한다.

**해소 조건**: 위 A/B/C 중 하나를 승인. C면 CI 런타임 확인부터, A·B면
설치 승인부터.
